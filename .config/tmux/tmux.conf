# options
set -g base-index 1
set -g renumber-windows on

# keybinds
set -g prefix C-Space
set -g mode-keys vi
bind l next-window
bind h previous-window

# status bar
set -g status-style "bg=default"
set -g window-status-current-style "fg=grey bold"
set -g status-interval 5
